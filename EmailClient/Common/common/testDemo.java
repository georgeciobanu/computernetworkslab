package common;

import java.io.BufferedReader;
import java.io.ObjectOutputStream;
import java.net.Socket;

public class testDemo {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
	/*	myMessageList msgList = new myMessageList();
		msgList.messages = new String[3];
		
		myContainer container = new myContainer();
		container.payload = msgList;
		container.msgType = messageTypes.MESSAGE_LIST;

		
		
		ObjectOutputStream objOutStream = new ObjectOutputStream(container);
		
		Socket sock = new Socket();
		BufferedReader r = new BufferedReader(sock.getOutputStream())
		
		*/
	}

}
